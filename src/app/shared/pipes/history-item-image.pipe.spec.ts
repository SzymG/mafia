import { HistoryItemImagePipe } from './history-item-image.pipe';

describe('HistoryItemImagePipe', () => {
  it('create an instance', () => {
    const pipe = new HistoryItemImagePipe();
    expect(pipe).toBeTruthy();
  });
});
